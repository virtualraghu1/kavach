{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm ci --prefix admin",
  "buildCommand": "npm --prefix admin run typecheck && npm --prefix admin run lint && npm --prefix admin test && npm --prefix admin run build",
  "outputDirectory": "admin/dist/client"
}
